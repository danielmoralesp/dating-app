<div class="row">
	<div class="small-12 small-centered columns logo">
		<%= image_tag "bg-white.png" %>
		<h1>Find your ottr half!</h1>
		<% if user_signed_in? %>

		<% else %>
			<%= link_to "Sign Up!", new_user_registration_path, class: "custom-button" %>
		<% end %>
	</div>
</div>