<br />

    <div class="row">

      <div class="large-12 small-12 columns">

        <%= image_tag(current_user.image.url(:medium)) %>

        <div class="panel">
          <h3><%= @current_user.username %></h3><hr />
          <h5 class="subheader"><%= @current_user.profile.bio %>
          </h5>
        </div>

      </div>
    </div>
    <%= link_to "Edit Profile", edit_profile_path(@current_user), class: "custom-button" %>
  </div>

</div>
