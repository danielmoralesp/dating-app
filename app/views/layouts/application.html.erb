<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

	<title><%= content_for?(:title) ? yield(:title) : "ottr" %></title>

	<%= stylesheet_link_tag    "application" %>
	<%= javascript_include_tag "application" %>
	<%= csrf_meta_tags %>
	<%= include_gon %>
</head>

<body>
	<nav class="top-bar" data-topbar data-no-turbolink role="navigation">
		<ul class="title-area">
			<li class="name">
				<h1><%= link_to "ottr", root_path %></h1>
			</li>
			<li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
		</ul>
		<section class="top-bar-section">
			<% if user_signed_in? %>

			<ul class="right">
				<li><%= link_to "Home", root_path %></li>
				<li class="has-dropdown">
					<a href="#">Account</a>
					<ul class="dropdown">
						<li><%= link_to "Profile", (profile_path current_user.profile.id) %></li>
						<li><%= link_to "Settings", (edit_user_path current_user) %></li>
						<li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
					</ul>
				</li>
			</ul>

			<% else %>
			<ul class="right">
				<li>Already have an account? &nbsp</li>
				<li class="active">
					<%= link_to "Log In", '#', data: {:'reveal-id' => 'loginModal'} %>
					<div id="loginModal" class="reveal-modal" data-reveal aria-hidden="true" role="dialog">
						<%= render 'users/sessions/new' %>
						<a class="close-reveal-modal">&#215;</a>
					</div>
				</li>
			</ul>
			<% end %>
		</section>
	</nav>

	

	<% if flash[:notice] %>
	<div class="notice"><%= flash[:notice] %></div>
	<% end %>
	<% if flash[:alert] %>
	<div class="alert"><%= flash[:alert] %></div>
	<% end %>

	<%= yield %>

</body>
</html>
